# D-Savor

Restaurant website template: menu pages, reservations, a photo gallery, and a contact form. Static HTML/CSS/JS with PHP handling the forms. An early project I keep around for reference.

## Running it

```bash
php -S localhost:8000
```

Then open http://localhost:8000.
